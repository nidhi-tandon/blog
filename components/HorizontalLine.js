const HorizontalLine = ({className}) => {
    return (
        <div
            className={className}
            style={{
                backgroundColor: "rgba(255,255,255,0.5)",
                height: "1px",
            }}

        />
    );
};

export default HorizontalLine;
